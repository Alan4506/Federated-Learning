@echo off
REM Batch file to install Python dependencies globally for the project

echo Installing dependencies from requirements.txt...
pip install -r requirements.txt

echo All dependencies installed. You can now run the Python scripts.
pause
